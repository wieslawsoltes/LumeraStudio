import {sqliteTable,text,integer,index,primaryKey} from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),name:text('name').notNull(),owner:text('owner').notNull(),scene:text('scene').notNull(),revision:integer('revision').notNull().default(1),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull()});
export const members=sqliteTable('members',{projectId:text('project_id').notNull(),userId:text('user_id').notNull(),name:text('name').notNull(),role:text('role').notNull(),lastSeen:integer('last_seen').notNull().default(0),selection:text('selection')},t=>[primaryKey({columns:[t.projectId,t.userId]}),index('members_user').on(t.userId)]);
export const invites=sqliteTable('invites',{token:text('token').primaryKey(),projectId:text('project_id').notNull(),role:text('role').notNull(),expiresAt:integer('expires_at').notNull()});
export const versions=sqliteTable('versions',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),name:text('name').notNull(),scene:text('scene').notNull(),author:text('author').notNull(),createdAt:integer('created_at').notNull()},t=>[index('versions_project').on(t.projectId,t.createdAt)]);
